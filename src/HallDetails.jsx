import React from 'react';

const HallDetails = ({ name }) => {
  return (
    <div>
      <h1>{name} - Function Hall Details</h1>
      <p>Details about {name} function hall...</p>
    </div>
  );
};

export default HallDetails;